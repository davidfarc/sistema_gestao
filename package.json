{
  "name": "ecco-sistema",
  "version": "0.0.0",
  "private": true,
  "description": "Sistema de Gestão Editorial — Editora Ecco Prime (monorepo)",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @ecco/web dev",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
